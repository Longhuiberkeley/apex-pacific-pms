import { useStore } from '../lib/store';
import { Sheet, Btn } from './Ui';
import WorkModes from './WorkMode';
export default function ModulesSheet(){const s=useStore();return <Sheet open={s.modulesOpen} onClose={()=>s.setModules(false)} title="How this demo works" width="w-[760px]"><div className="space-y-5 p-5"><p className="text-sm text-muted">Presenter information. The normal workflow starts in Today or a fund record.</p><WorkModes/><section className="rounded border border-line p-4"><h2 className="font-semibold">A single fund record</h2><p className="mt-2 text-sm">Research, original files, approved data, monitoring, fees and decisions stay attached to the fund. Today brings outstanding work from those records into one queue.</p></section><section className="rounded border border-line p-4"><h2 className="font-semibold">What works in this demonstration</h2><p className="mt-2 text-sm">Editable screening, document review, saved data, role restrictions, agent submissions, review handoffs, monitoring checks, fee reconciliation and portfolio approvals.</p><p className="mt-2 text-sm text-muted">Research and document extraction use prepared examples. External agents can submit real commands through the local CLI. This is a browser-session demo: refresh resets work; role restrictions are illustrative.</p></section><div className="flex gap-3"><Btn onClick={()=>{s.setModules(false);s.setShell(true);}}>Open agent console</Btn><Btn onClick={()=>s.setIntakePolicy(s.intakePolicy==='AUTO'?'MANUAL':'AUTO')}>Document review: {s.intakePolicy==='AUTO'?'Quick approval when confident':'Full review for every document'}</Btn></div><p className="text-xs text-muted">Investment execution, live email delivery, incentive fees and persistent storage are outside this demonstration.</p></div></Sheet>;}
